{
  "name": "<%= slugify(config.get('presentationTitle')) %>",
  "version": "1.0.0",
  "private": true,
  "dependencies": {  },
  "devDependencies": {
    "directory-tree": "^1.2.0",
    "gulp": "^3.9.1",
    "gulp-batch": "^1.0.5",
    "gulp-template": "^4.0.0",
    "gulp-watch": "^4.3.11",
    "gulp-webserver": "^0.9.1"
  },
  "engines": {
    "node": ">=4"
  },
  "scripts": {
    "test": "grunt test"
  }
}
